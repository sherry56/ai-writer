services:
  ai-writer:
    build: .
    image: ai-writer:latest
    container_name: ai-writer
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config
